generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Models will be added as we implement features.
// See openspec/changes/multi-operator-trip-booking-app/ for the planned schema.
