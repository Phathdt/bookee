# E2E test environment
TEST_ENVIRONMENT=local
HEADLESS=true
CUCUMBER_PARALLEL=2

# Targets — point at the FE app under test
APP_URL=http://localhost:5174
API_URL=http://localhost:3000/api/v1

# Tracing — off | on | retain-on-failure
TRACE=retain-on-failure
LOG_LEVEL=info
